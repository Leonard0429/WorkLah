<style>
    :root {
        --wl-blue: #1d4ed8;
        --wl-blue-dark: #1e3a8a;
        --wl-blue-light: #dbeafe;
        --wl-blue-border: #93c5fd;
        --wl-yellow: #facc15;
        --wl-yellow-dark: #eab308;
        --wl-yellow-text: #78350f;
        --wl-yellow-bg: #fef9c3;
    }

    body {
        background-color: #f8fafc;
    }

    .navbar-worklah {
        background-color: var(--wl-blue);
    }

    .navbar-worklah .navbar-brand {
        color: var(--wl-yellow);
        font-weight: 600;
    }

    .navbar-worklah .nav-link {
        color: #e0e7ff;
    }

    .navbar-worklah .nav-link:hover {
        color: #ffffff;
    }

    .btn-worklah {
        background-color: var(--wl-yellow);
        color: var(--wl-yellow-text);
        border: none;
        font-weight: 500;
    }

    .btn-worklah:hover {
        background-color: var(--wl-yellow-dark);
        color: var(--wl-yellow-text);
    }

    .page-title {
        color: var(--wl-blue-dark);
    }

    .hero-worklah {
        background-color: var(--wl-blue-light);
        padding: 3rem 1.5rem;
        text-align: center;
        border-radius: 12px;
    }

    .hero-worklah h1 {
        color: var(--wl-blue-dark);
    }

    .hero-worklah p {
        color: #3b5aa0;
    }

    .worklah-card,
    .card-gig {
        background-color: #ffffff;
        border: 1px solid var(--wl-blue-border);
        border-radius: 12px;
    }

    .worklah-card-header {
        background-color: var(--wl-blue);
        color: #ffffff;
    }

    .form-card {
        max-width: 760px;
        margin-left: auto;
        margin-right: auto;
    }

    .badge-category {
        background-color: var(--wl-blue-light);
        color: #1e40af;
    }

    .status-pending {
        background-color: var(--wl-yellow-bg);
        color: #854d0e;
    }

    .status-accepted {
        background-color: #dcfce7;
        color: #166534;
    }

    .status-rejected {
        background-color: #fee2e2;
        color: #991b1b;
    }

    .footer-worklah {
        background-color: var(--wl-blue-dark);
        color: #bfdbfe;
        text-align: center;
        padding: 1.25rem;
        margin-top: 3rem;
        font-size: 0.9rem;
    }

    .profile-picture,
    .profile-picture-placeholder {
        width: 110px;
        height: 110px;
        border: 3px solid var(--wl-blue-border);
        border-radius: 50%;
    }

    .profile-picture {
        object-fit: cover;
    }

    .profile-picture-placeholder {
        display: flex;
        align-items: center;
        justify-content: center;
        background-color: var(--wl-blue-light);
        color: var(--wl-blue-dark);
        font-size: 2.5rem;
        font-weight: 600;
    }

    .profile-picture-small {
        width: 72px;
        height: 72px;
        flex-shrink: 0;
        font-size: 1.75rem;
    }
</style>
