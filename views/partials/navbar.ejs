<nav class="navbar navbar-expand-lg navbar-dark navbar-worklah">
    <div class="container">
        <a class="navbar-brand" href="/">WorkLah</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMenu"
            aria-controls="navbarMenu" aria-expanded="false" aria-label="Open navigation">

            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarMenu">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/">Home</a>
                </li>

                <% if (user) { %>
                    <% if (user.role === 'admin') { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/admin">Admin</a>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/profile">Profile</a>
                        </li>
                    <% } %>

                    <li class="nav-item">
                        <a class="nav-link" href="/joblist">Jobs</a>
                    </li>

                    <% if (user.role === 'student') { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/bookmarks">Saved Jobs</a>
                        </li>
                    <% } %>

                    <li class="nav-item">
                        <a class="nav-link" href="/applicationList">
                            <%= user.role === 'admin'
                                ? 'Applications'
                                : 'My Applications' %>
                        </a>
                    </li>
                <% } %>
            </ul>

            <ul class="navbar-nav align-items-lg-center gap-lg-2">
                <% if (user) { %>
                    <li class="nav-item">
                        <a class="btn btn-worklah btn-sm" href="/logout">Logout</a>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Login</a>
                    </li>

                    <li class="nav-item">
                        <a class="btn btn-worklah btn-sm" href="/register">Register</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
